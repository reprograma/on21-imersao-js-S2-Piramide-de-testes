describe('empty spec', () => {
  it('passes', () => {
    cy.visit('https://example.cypress.io')
  })
})

describe("Meu primeiro teste", () =>{
  it("passar por favor", () => {
    expect(true).to.equal(true)
  })
})