describe('empty spec', () => {
  it('passes', () => {
    cy.visit('https://example.cypress.io')
  })
})

describe("Meu primeiro teste com Cypress", () => {
  it("", () => {
    expect(true).to.equal(true)
  })
})
