describe('empty spec', () => {
  it('passes', () => {
    cy.visit('https://example.cypress.io') // cypress visita essa página
  })
})

describe("Meu primero teste", () => {
  it("passa por favor", () => {
    expect(true).to.equal(true)
  })
});